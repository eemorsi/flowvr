pdi:
  logging: debug

  data:
    wait: int
    scalar: int

  plugins:
    flowvr:
      component: module
      wait_on_data: wait
      abort_on_finalize: true
      input_ports:
        text:
          data: scalar

