pdi:
  logging: debug

  data:
    wait: int
    scalar: int

  plugins:
    flowvr:
      component: module
      wait_on_data: wait
      output_ports:
        text: # port_name 
          data: scalar
